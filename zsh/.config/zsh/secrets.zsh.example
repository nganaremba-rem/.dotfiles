# Example secrets file — TEMPLATE, committed to the repo.
#
# Setup:
#   cp secrets.zsh.example secrets.zsh   # then edit secrets.zsh with real values
#
# ~/.zshrc sources ~/.config/zsh/secrets.zsh if it exists. The real secrets.zsh
# is gitignored; only this .example is tracked.

export ANTHROPIC_API_KEY="sk-ant-REPLACE_ME"
